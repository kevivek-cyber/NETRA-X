{
  "name": "netra-x-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "NETRA-X desktop shell (Tauri) — local-first packaging on top of apps/api and apps/web",
  "scripts": {
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0"
  }
}
